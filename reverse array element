#include<iostream>
using namespace std;
int main()
{
     int limit;
    cin>>limit;
    int arr[limit];
    for (int i = 0; i < limit; i++)
    {
        cin>>arr[i];
    }
    for (int j = limit-1; j >= 0; j--)
    {
        cout<<arr[j]<<" ";
    }
    return 0;
    
    
}
